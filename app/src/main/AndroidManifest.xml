<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="java.com.example.mynews.MyNewsApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyNews" >

    <activity android:name="java.com.example.mynews.ui.MainActivity"
        android:exported="true"
        android:windowSoftInputMode="adjustResize">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE" />
            <data
                android:host = "developer.android.com"
                android:pathPrefix = "/jetnews"
                android:scheme="https" />
        </intent-filter>
    </activity>

    <receiver
        android:name="java.com.example.mynews.glance.MyNewsGlanceAppWidgetReceiver"
        android:exported="false"
        android:enabled="true">
        <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
        </intent-filter>
    <meta-data
        android:name="com.example.android.glance.appwidget.provider"
        android:resource="@xml/mynews_glance_appwidget_info" />
    </receiver>
    </application>
</manifest>